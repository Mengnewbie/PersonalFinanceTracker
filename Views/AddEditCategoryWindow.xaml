<Window x:Class="PersonalFinanceTracker.Views.AddEditCategoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Category" 
        Height="496" 
        Width="736"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Add New Category" 
                   FontSize="24" 
                   FontWeight="Bold"
                   Foreground="#2C3E50"
                   Margin="0,0,0,20"/>

        <!-- Name -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Category Name" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="NameTextBox" 
                     FontSize="14"
                     Padding="8"
                     Height="35"/>
        </StackPanel>

        <!-- Type -->
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Type" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <ComboBox x:Name="TypeComboBox" 
                      FontSize="14"
                      Padding="8"
                      Height="35">
                <ComboBoxItem Content="Income" IsSelected="True"/>
                <ComboBoxItem Content="Expense"/>
            </ComboBox>
        </StackPanel>

        <!-- Icon -->
        <StackPanel Grid.Row="3" Margin="0,0,0,15">
            <TextBlock Text="Icon (Emoji)" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="IconTextBox" 
                     FontSize="14"
                     Padding="8"
                     Height="35"
                     MaxLength="2"
                     Text="📁"/>
        </StackPanel>

        <!-- Color -->
        <StackPanel Grid.Row="4" Margin="0,0,0,15">
            <TextBlock Text="Color (Hex)" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="ColorTextBox" 
                     FontSize="14"
                     Padding="8"
                     Height="35"
                     Text="#3498DB"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="6" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="Cancel" 
                    Width="100"
                    Height="35"
                    Margin="0,0,10,0"
                    FontSize="14"
                    Click="CancelButton_Click"
                    Background="#95A5A6"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#95A5A6"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#7F8C8D"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <Button Content="Save" 
                    Width="100"
                    Height="35"
                    FontSize="14"
                    Click="SaveButton_Click"
                    Background="#27AE60"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#27AE60"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#229954"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
