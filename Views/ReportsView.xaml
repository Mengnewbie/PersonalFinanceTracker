<UserControl x:Class="PersonalFinanceTracker.Views.ReportsView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF" xmlns:viewmodels="clr-namespace:PersonalFinanceTracker.ViewModels" Background="Transparent">
    <UserControl.DataContext>
        <viewmodels:ReportsViewModel/>
    </UserControl.DataContext>
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="0,0,0,20">
            <Grid Margin="0,0,0,20">
                <TextBlock Text="Reports &amp; Analytics" Foreground="{StaticResource TextBrush}" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock Text="Period:" FontSize="13" Foreground="{StaticResource Text3Brush}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <ComboBox ItemsSource="{Binding TimePeriods}" SelectedItem="{Binding SelectedPeriod}" Width="160" Height="36" FontSize="13" Padding="10,5"/>
                </StackPanel>
            </Grid>
            <!-- Stat Cards -->
            <UniformGrid Rows="1" Columns="4" Margin="0,0,0,20">
                <Border Background="{StaticResource GreenDimBrush}" BorderBrush="#1A3D34" BorderThickness="1" CornerRadius="14" Padding="18" Margin="0,0,6,0">
                    <StackPanel>
                        <TextBlock Text="INCOME" FontSize="10" FontWeight="SemiBold" Foreground="{StaticResource GreenBrush}" Opacity="0.8" Margin="0,0,0,8"/>
                        <TextBlock Text="{Binding TotalIncomeFormatted}" Foreground="{StaticResource GreenBrush}" FontSize="22" FontWeight="Bold" Margin="0,0,0,4"/>
                        <TextBlock Foreground="{StaticResource Text3Brush}" FontSize="11"><Run Text="{Binding IncomeTransactions, Mode=OneWay}"/><Run Text=" transactions"/></TextBlock>
                    </StackPanel>
                </Border>
                <Border Background="{StaticResource RedDimBrush}" BorderBrush="#3D1A22" BorderThickness="1" CornerRadius="14" Padding="18" Margin="3,0,3,0">
                    <StackPanel>
                        <TextBlock Text="EXPENSES" FontSize="10" FontWeight="SemiBold" Foreground="{StaticResource RedBrush}" Opacity="0.8" Margin="0,0,0,8"/>
                        <TextBlock Text="{Binding TotalExpensesFormatted}" Foreground="{StaticResource RedBrush}" FontSize="22" FontWeight="Bold" Margin="0,0,0,4"/>
                        <TextBlock Foreground="{StaticResource Text3Brush}" FontSize="11"><Run Text="{Binding ExpenseTransactions, Mode=OneWay}"/><Run Text=" transactions"/></TextBlock>
                    </StackPanel>
                </Border>
                <Border Background="{StaticResource BlueDimBrush}" BorderBrush="#2A2450" BorderThickness="1" CornerRadius="14" Padding="18" Margin="3,0,3,0">
                    <StackPanel>
                        <TextBlock Text="NET SAVINGS" FontSize="10" FontWeight="SemiBold" Foreground="{StaticResource BlueBrush}" Opacity="0.8" Margin="0,0,0,8"/>
                        <TextBlock Text="{Binding NetSavingsFormatted}" Foreground="{StaticResource BlueBrush}" FontSize="22" FontWeight="Bold" Margin="0,0,0,4"/>
                        <TextBlock Foreground="{StaticResource Text3Brush}" FontSize="11"><Run Text="{Binding SavingsRate, StringFormat={}{0:F1}, Mode=OneWay}"/><Run Text="% rate"/></TextBlock>
                    </StackPanel>
                </Border>
                <Border Background="{StaticResource PurpleDimBrush}" BorderBrush="#2E2450" BorderThickness="1" CornerRadius="14" Padding="18" Margin="6,0,0,0">
                    <StackPanel>
                        <TextBlock Text="DAILY AVG" FontSize="10" FontWeight="SemiBold" Foreground="{StaticResource PurpleBrush}" Opacity="0.8" Margin="0,0,0,8"/>
                        <TextBlock Text="{Binding DailyAverageFormatted}" Foreground="{StaticResource PurpleBrush}" FontSize="22" FontWeight="Bold" Margin="0,0,0,4"/>
                        <TextBlock Foreground="{StaticResource Text3Brush}" FontSize="11"><Run Text="{Binding DaysAnalyzed, Mode=OneWay}"/><Run Text=" days"/></TextBlock>
                    </StackPanel>
                </Border>
            </UniformGrid>
            <!-- Charts Row -->
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="18" Margin="0,0,8,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Income vs Expenses" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,12"/>
                        <lvc:CartesianChart Grid.Row="1" Series="{Binding IncomeExpenseBarSeries}" XAxes="{Binding IncomeExpenseXAxes}" LegendPosition="Bottom" LegendTextSize="11" Height="200"/>
                    </Grid>
                </Border>
                <Border Grid.Column="1" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="18" Margin="4,0,4,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Expense Breakdown" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,12"/>
                        <lvc:PieChart Grid.Row="1" Series="{Binding ExpensePieSeries}" LegendPosition="Right" LegendTextSize="10" InitialRotation="-90" Height="200"/>
                    </Grid>
                </Border>
                <Border Grid.Column="2" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="18" Margin="8,0,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Income Sources" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,12"/>
                        <lvc:PieChart Grid.Row="1" Series="{Binding IncomePieSeries}" LegendPosition="Right" LegendTextSize="10" InitialRotation="-90" Height="200"/>
                    </Grid>
                </Border>
            </Grid>
            <!-- Monthly Trend -->
            <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="22" Margin="0,0,0,20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Monthly Trend" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,16"/>
                    <lvc:CartesianChart Grid.Row="1" Series="{Binding MonthlyTrendSeries}" XAxes="{Binding MonthlyTrendXAxes}" LegendPosition="Top" LegendTextSize="12" Height="280"/>
                </Grid>
            </Border>
            <!-- Category Trend + Day of Week -->
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="22" Margin="0,0,10,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Top Categories Over Time" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,16"/>
                        <lvc:CartesianChart Grid.Row="1" Series="{Binding CategoryTrendSeries}" XAxes="{Binding CategoryTrendXAxes}" LegendPosition="Top" LegendTextSize="11" Height="260"/>
                    </Grid>
                </Border>
                <Border Grid.Column="1" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="22" Margin="10,0,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Spending by Day" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,16"/>
                        <lvc:CartesianChart Grid.Row="1" Series="{Binding DayOfWeekSeries}" XAxes="{Binding DayOfWeekXAxes}" LegendPosition="Hidden" Height="260"/>
                    </Grid>
                </Border>
            </Grid>
            <!-- Category Stats + Income Sources -->
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="22" Margin="0,0,10,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Category Performance" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,16"/>
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" MaxHeight="300">
                            <ItemsControl ItemsSource="{Binding CategoryStats}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{StaticResource Surface2Brush}" CornerRadius="10" Padding="12" Margin="0,0,0,6">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <Grid Grid.Row="0" Margin="0,0,0,8">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Border Width="28" Height="28" CornerRadius="8" Background="{StaticResource Surface3Brush}" Margin="0,0,8,0">
                                                            <TextBlock Text="{Binding Icon}" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                        <StackPanel VerticalAlignment="Center">
                                                            <TextBlock Text="{Binding Category}" FontWeight="SemiBold" FontSize="13" Foreground="{StaticResource TextBrush}"/>
                                                            <TextBlock FontSize="10" Foreground="{StaticResource Text3Brush}"><Run Text="{Binding Count, Mode=OneWay}"/><Run Text=" txns"/></TextBlock>
                                                        </StackPanel>
                                                    </StackPanel>
                                                    <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding TotalFormatted}" FontSize="14" FontWeight="Bold" Foreground="{StaticResource RedBrush}" HorizontalAlignment="Right"/>
                                                        <TextBlock FontSize="11" Foreground="{StaticResource Text3Brush}" HorizontalAlignment="Right"><Run Text="{Binding Percentage, StringFormat={}{0:F1}, Mode=OneWay}"/><Run Text="%"/></TextBlock>
                                                    </StackPanel>
                                                </Grid>
                                                <ProgressBar Grid.Row="1" Height="4" Value="{Binding Percentage}" Maximum="100" Background="#252838" Foreground="{Binding Color}" BorderThickness="0"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>
                <Border Grid.Column="1" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="22" Margin="10,0,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Income Sources" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,16"/>
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" MaxHeight="300">
                            <ItemsControl ItemsSource="{Binding IncomeSources}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{StaticResource Surface2Brush}" CornerRadius="10" Padding="12" Margin="0,0,0,6">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <Grid Grid.Row="0" Margin="0,0,0,8">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Border Width="28" Height="28" CornerRadius="8" Background="{StaticResource Surface3Brush}" Margin="0,0,8,0">
                                                            <TextBlock Text="{Binding Icon}" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                        <StackPanel VerticalAlignment="Center">
                                                            <TextBlock Text="{Binding Source}" FontWeight="SemiBold" FontSize="13" Foreground="{StaticResource TextBrush}"/>
                                                            <TextBlock FontSize="10" Foreground="{StaticResource Text3Brush}"><Run Text="{Binding Count, Mode=OneWay}"/><Run Text=" txns"/></TextBlock>
                                                        </StackPanel>
                                                    </StackPanel>
                                                    <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding TotalFormatted}" FontSize="14" FontWeight="Bold" Foreground="{StaticResource GreenBrush}" HorizontalAlignment="Right"/>
                                                        <TextBlock FontSize="11" Foreground="{StaticResource Text3Brush}" HorizontalAlignment="Right"><Run Text="{Binding Percentage, StringFormat={}{0:F1}, Mode=OneWay}"/><Run Text="%"/></TextBlock>
                                                    </StackPanel>
                                                </Grid>
                                                <ProgressBar Grid.Row="1" Height="4" Value="{Binding Percentage}" Maximum="100" Background="#252838" Foreground="{Binding Color}" BorderThickness="0"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>
            <!-- Key Insights -->
            <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="22">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="💡" FontSize="28" VerticalAlignment="Top" Margin="0,0,16,0"/>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Key Insights" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,12"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,20,0">
                                <TextBlock FontSize="13" Foreground="{StaticResource Text2Brush}" Margin="0,0,0,6"><Run Text="Most Used: " FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/><Run Text="{Binding MostUsedCategory, Mode=OneWay}"/></TextBlock>
                                <TextBlock FontSize="13" Foreground="{StaticResource Text2Brush}" Margin="0,0,0,6"><Run Text="Avg Income: " FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/><Run Text="{Binding AverageIncomeFormatted, Mode=OneWay}"/></TextBlock>
                                <TextBlock FontSize="13" Foreground="{StaticResource Text2Brush}"><Run Text="Avg Expense: " FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/><Run Text="{Binding AverageExpenseFormatted, Mode=OneWay}"/></TextBlock>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock FontSize="13" Foreground="{StaticResource Text2Brush}" Margin="0,0,0,6"><Run Text="Largest Income: " FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/><Run Text="{Binding LargestIncomeFormatted, Mode=OneWay}"/></TextBlock>
                                <TextBlock FontSize="13" Foreground="{StaticResource Text2Brush}" Margin="0,0,0,6"><Run Text="Largest Expense: " FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/><Run Text="{Binding LargestExpenseFormatted, Mode=OneWay}"/></TextBlock>
                                <TextBlock FontSize="13" Foreground="{StaticResource Text2Brush}"><Run Text="Total: " FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/><Run Text="{Binding TotalTransactions, Mode=OneWay}"/><Run Text=" transactions"/></TextBlock>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Grid>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
